# NixOS configuration

This is my NixOS configuration so that I can easily and quickly bootstrap
NixOS from a flake anytime I need.

Currently hosts two configurations, `main` which is my PC and `flex` which
is my laptop that I sometimes use.
